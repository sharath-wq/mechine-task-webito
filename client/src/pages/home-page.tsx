import Navbar from '@/components/navbar';
import Table from '@/components/table';

const HomePage = () => {
    return (
        <>
            <Navbar />
            <Table />
        </>
    );
};

export default HomePage;
